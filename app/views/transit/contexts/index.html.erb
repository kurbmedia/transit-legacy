<ul id="context_list" class='contents'>
	<%= form.fields_for :contexts do |cform| %>
	<%= render(template: 'transit/contexts/edit', locals: { context: cform.object, form: cform, parent: form } ) %>
	<% end %>
</ul>